services:
  ubuntu:
    build: 
      context: .
      dockerfile: dockerfile
    image: ubuntu
    tty: true
    stdin_open: true
    restart: always
    environment:
      TZ: Asia/Taipei
    networks:
      npm:

networks:
  npm:
    external: true