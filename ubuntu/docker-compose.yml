services:
  ubuntu:
    build: 
      context: .
      dockerfile: dockerfile
    image: ubuntu_node_20241122_2329
    tty: true
    stdin_open: true
    restart: always
    environment:
      TZ: Asia/Taipei
    networks:
      npm:

networks:
  npm:
    external: true