services:
  ubuntu:
    build: 
      context: .
      dockerfile: dockerfile
      args:
        CACHE_BUST: "${CACHE_BUST:-$(date +%s)}"
    image: ubuntu
    tty: true
    stdin_open: true
    restart: always
    environment:
      TZ: Asia/Taipei
    networks:
      npm:

networks:
  npm:
    external: true