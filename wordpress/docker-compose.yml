services:
  wordpress:
    image: wordpress:latest
    restart: always
    extra_hosts:
      - host.docker.internal:host-gateway
    dns:
      - 172.18.0.1
    volumes:
      - /volume1/docker/wordpress/html:/var/www/html
    networks:
      - npm

networks:
  npm:
    external: true
